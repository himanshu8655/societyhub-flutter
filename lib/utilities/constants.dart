import 'package:flutter/material.dart';

class Constants {
  /// FONTS
  static const FONT_OPENSANS = 'OpenSans';
  static const FONT_RALEWAY = 'OpenSans';

  ///Duration
  static const duration2000 = Duration(milliseconds: 2000);

  ///ThemeData
  static final theme = ThemeData(
    fontFamily: Constants.FONT_OPENSANS,
    // unselectedWidgetColor: AppColors.blue
  );
}
